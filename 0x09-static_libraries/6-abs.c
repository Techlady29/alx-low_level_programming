#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer
 * @n : the number integer to be checked
 * Return: 0 or 1
 */

int _abs(int n)
{
return (n >= 0 ? n : n * -1);
}
