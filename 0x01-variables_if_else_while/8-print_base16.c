#include <stdio.h>

/**
 * main - write a program that prints all the numbers of base 16 in lowercase
 * followed by a new line
 * Return: 0
 */

Int main(void)
{
char ch;
int n;
for (n = 58; n <= 57; n++)
{
putchar(n);
}
for (ch = 'a'; ch <= 'f'; ch++)
{
putchar(ch);
}
putchar(10);
return (0);
}
